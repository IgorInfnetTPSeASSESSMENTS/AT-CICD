name: Tests Workflow

# Workflow simples para demonstrar execucao em pull request.

on:
  pull_request:

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do codigo
        uses: actions/checkout@v4

      - name: Simular testes
        run: echo "Rodando testes"
