name: Env Demo

on:
  push:

jobs:
  env-demo:
    runs-on: ubuntu-latest
    env:
      DEPLOY_ENV: staging
    steps:
      - name: Exibir ambiente
        run: echo "Ambiente de deploy: $DEPLOY_ENV"
